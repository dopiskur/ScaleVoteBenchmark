## .NET
bin/
obj/
*.user
*.suo
.vs/

## Rider / VS Code
.idea/
.vscode/

## Claude Code
.claude/

## appsettings s tajnama - lokalne verzije se ne commitaju
## Predlošci (appsettings.json.example) ostaju trackani, stvarne datoteke ne.
appsettings.json
appsettings.Development.json
appsettings.Local.json

## Local SQLite database file (DatabaseProvider = Sqlite)
*.db
*.db-shm
*.db-wal

## Deploy.ps1 local config - template (config.json.example) stays tracked, the real file does not.
config.json

## OS
.DS_Store
Thumbs.db

## Python (scripts/)
__pycache__/
*.pyc

## Run-ScalingScenarios.ps1 output (CSVs, HTML report, load logs, azcontext.json) -
## -Output has no fixed default, this covers the common case of a sibling "results" folder
deploy/azure-demo-resources/results/
